node_modules/
dist/
*.log
.DS_Store
Thumbs.db

# Secrets and env (allow .env.example so contributors see the template)
.env
.env.*
!.env.example
.envrc
*.pem
*.key
*.p12

# Local scratch
*.local

# Session-launch prompts dropped at repo root by host CLI integrations
TODO.md

# Bun (legacy binary lockfile; bun.lock text format is committed)
bun.lockb

# IDE
.vscode/
.idea/

# remember plugin (Claude Code) — per-project session-state cache
.remember/

# Serena MCP — per-project symbol index and cache (created by activate_project)
.serena/

# code-oz runtime artifacts (when run inside another project; not in our repo)
.code-oz/runs/
.code-oz/state/events.jsonl
.code-oz/state/current.json
.code-oz/state/NEEDS_INTERVENTION.json
.code-oz/state/PAUSE.json
.code-oz/state/STOP.json

# W3-lite Ralph loop session-local state (overnight 2026-05-01)
.code-oz/state/ralph-state.md
.code-oz/state/ralph-summary.md
.code-oz/state/RALPH_HALT.md
.code-oz/state/W3_LITE_DONE.json
