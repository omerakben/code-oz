# Template folder comparison — 2026-05-01

Surveyed all 16 immediate folders under `~/Projects/agents/templates/` for the inter-milestone refactor session (between M12 and PE-1). Goal: find patterns code-oz should consider borrowing now, defer, or reject — without scope-creeping the locked PE-1 plan.

## Summary

- 16 folders surveyed in parallel by four `Explore` agents (4 each).
- **Already borrowed** (confirmed against CLAUDE.md influence-library): 7 — agent-skills, opencode, Archon, pi-mono, maestro, Auto-claude-code-research-in-sleep, claude-code.
- **First-time inclusion gate (byterover-cli):** review verdict `defer / borrow-later`. Multi-provider Vercel-AI-SDK pattern is interesting; daemon + bundled UI conflict with code-oz file-based-gates discipline. Permission manifest is absent. Re-evaluate after PE-1 ships if friend-survey demand surfaces routed retail access.
- **Provenance hit (must update CLAUDE.md exclusion list):** `agentic-coder/` ships only `src/` with import surface (`@anthropic-ai/sdk/resources/index.mjs`, `bun:bundle`, `accumulateUsage`, `EMPTY_USAGE`, internal `src/services/api/claude.js`) that matches the publicly leaked Claude Code source dated 2026-03-31. CLAUDE.md exclusion currently names only `claude-code-main` (no longer present in templates/). Treating as same provenance issue, relabeled. **Hard reject; pattern borrow prohibited; clean-room separation required as before.**
- **Borrow-now candidates: zero.** No template surfaces a pattern that beats existing code-oz contracts at the next milestone (PE-1: xAI direct HTTP). The maturity gap that opencode + Archon + pi-mono already fill (compile, worktree, streaming) is closed.

## Comparison matrix

| Folder | Stack & PM | Workflow / lifecycle pattern | Agent / provider model | Permission / sandbox / trust pattern | Context / search / memory pattern | Review / debate / verification pattern | code-oz already borrowed | Worth borrowing now | Reject / defer | Files inspected | Adoption risk |
|---|---|---|---|---|---|---|---|---|---|---|---|
| agent-skills | Node 18+, npm; Markdown SKILL.md frontmatter; no runtime deps | Six-phase SDLC (DEFINE→PLAN→BUILD→VERIFY→REVIEW→SHIP); slash-command activation; pre/post hooks; Common-Rationalizations + Red Flags + per-skill verification gates | Multi-persona via subagent text (code-reviewer, security-auditor); no provider abstraction | No sandbox; works under host CLI permission models (Claude Code, Cursor, etc.) | Checklists in `references/`; progressive disclosure (name+desc auto-load, body on-demand) | Five-axis review framing in `code-review`; anti-rationalization tables; evidence-driven verification | Frontmatter format + DEFINE→SHIP phases + Common-Rationalizations | Fan-out (`/ship` multi-reviewer) — defer until M14 panel design round | First-class persona-to-persona routing (violates code-oz orchestrator authority) | README, AGENTS.md, hooks/, .claude/commands/ | Low; surface-only |
| agentic-canvas | Vanilla HTML/JS + vendored Drawflow; Node 14+; no build | DAG editor producing JSON schema v0.4 with 11 node types; auto-layout (Dagre/ELK); browser-only | Schema-only; agent role + acceptance-criteria fields per node; no provider | No HTTP, no auth, no permission manifest; browser-implicit | File-based JSON load/save via `canvasctl.mjs`; no memory | None — design-time tool only | None | Schema vocabulary may inform future visual workflow preview | Drawflow dependency (UI lock-in); Canvas-as-execution-layer | CLAUDE.md, README.md, schemas/agent-canvas.schema.json | Low; orthogonal use case |
| **agentic-coder** | TypeScript + React/Ink TUI; npm; no LICENSE/README/package.json | Internal Anthropic Claude Code architecture | n/a (proprietary, leaked) | n/a | n/a | n/a | n/a | **Reject (hard).** Filenames + import surface match the 2026-03-31 leaked .map. CLAUDE.md exclusion list names only `claude-code-main` (now removed); this folder is the same provenance under a relabel. | Add to CLAUDE.md influence-library exclusion. Pattern borrow prohibited. | src/Tool.ts, src/QueryEngine.ts, src/costHook.ts (file list only) | **Critical (provenance).** |
| agenticSeek | Python 3.10+; FastAPI + Flask; Docker (SearxNG, Redis, Ollama) | Autonomous web-browsing + task decomposition + agent-router selection; imperative Python control flow | Adaptive-classifier router selects best agent per query; multi-LLM via env keys | Privacy-first local execution; WORK_DIR isolation; Docker service isolation | Local SearxNG search; stateless agent selection (no persistent memory) | None — autonomous loop, no approval gates | None (paradigm mismatch) | Task-decomposition pattern may inform M15 debate-policy heuristics — defer | Stateless conflicts with code-oz workflow persistence | README, pyproject.toml, api.py, cli.py | High; paradigm mismatch |
| Archon | Bun + TS monorepo; bunfig.toml; Workspaces | YAML DAG workflows (plan→implement→validate→review→PR); worktree-per-run; parallel via git worktrees | IAgentProvider interface with Zod-typed schemas; FakeProvider for determinism | Permission gates per node; one-authority-per-milestone discipline; fail-fast | Per-concern engine schemas (dag-node, workflow, run); loader validates DAG cycles | Cross-provider review nodes; output validation; approval gates in DAG | IAgentProvider + worktree isolation + Zod schema architecture | M14 panel quorum may borrow Archon DAG approval-node shape — defer | DAG cycling constraints; spec-first non-negotiable | CLAUDE.md, package.json, IAgentProvider.ts | Low; already-borrowed |
| Auto-claude-code-research-in-sleep | Markdown skills + plain-text artifacts; provider-agnostic Claude/Codex/Kimi/MiniMax/GLM | 4-phase research workflow (idea→research→writing→rebuttal); effort levels (lite/balanced/max/beast) × assurance gates (draft/submission); /research-pipeline | Multi-executor routing; cross-model adversarial review via Codex MCP | Effort-gated permission tiers; assurance gates (draft preview vs submission lock) | Research Wiki persistent memory; Reviewer Memory (4-round cap); paper illustration adapters | Cross-family review via Codex MCP; 4-round debate cap; effort+assurance gates; plain Markdown artifacts | Cross-family review + 4-round cap + plain-Markdown contracts + Reviewer Memory | Effort levels for VERIFY/REVIEW depth — defer until M14 panel design | Skill-based dispatch (would add new orchestration surface; M16+) | README, AGENT_GUIDE.md | Low; reference only |
| byterover-cli | oclif v4 (Node 20+, ESM); npm; React/Ink/Zustand; @ai-sdk/* (18 LLM providers); strict TDD (Mocha+Chai+Sinon+Nock 80%) | REPL + TUI (`brv` interactive); web dashboard (Express + Socket.IO); daemon + forked agent pool; git-like VC subsystem (init/commit/branch/...); worktrees (`projectRoot`/`worktreeRoot`) | IAgentProvider over 18 providers (anthropic/openai/google/groq/mistral/xai/cerebras/...); Vercel SDK abstractions; multi-provider memory federation | OAuth + API key per provider; Socket.IO daemon (CSP/TLS-aware); HTTP testing via nock; **no permission manifest documented**; no sandbox isolation | BM25 search (minisearch); query-log store; ByteRover Cloud sync; Hub ecosystem (`brv hub`) | `brv review pending/approve/reject` is approval workflow only; no debate runtime; no Opus-default; no cross-family enforcement | None (first-time inclusion candidate) | Multi-provider abstraction (Vercel SDK, 18 providers) — pattern only, defer; PE-1 only needs xAI direct | Daemon architecture (conflicts with file-based gates rule); bundled UI (conflicts with single-binary); approval-only review (not debate-capable); permission manifest missing | CLAUDE.md, README, package.json | **Medium-High** (provenance clean Elastic 2.0; arch divergence + missing permission manifest are blockers) |
| claude-code | Bun + TS; npm + Homebrew + Scoop; single-file binary | Plugin ecosystem + hook event names + filesystem discovery (.claude/plugins, .claude-plugin/plugin.json) | Plugin-provider via hook dispatch; commands/agents/skills in manifest; no native provider abstraction | Per-command permission settings (allowed Bash, network, file roots, env, secrets, timeout); MCP integration | Plugin-scoped hook runners; filesystem discovery at boot; session-scoped context | No formal verification gate; debug hooks on error | Plugin format + hook event names + filesystem discovery | Hook event naming for phase lifecycle (`tool_use`, `phase_end`, `verification_gate`) — defer until M14 needs panel hooks | Plugin registry (would add new authority — M16+) | README, plugins/ | Low; surface-only |
| claude-coder | TypeScript + Node 18+; pnpm; VS Code extension (esbuild) | UI-driven agentic; webview IPC; full-project glob scan on open | Single provider (Claude); linear conversation | VS Code extension sandbox; embedded Anthropic OAuth in extension | Glob-based codebase indexing; no cross-session memory | Debug via VS Code debug console; no cross-model review | None | None — IDE-bound, single-provider, paradigm mismatch | Strong defer; embedded OAuth + extension dependency incompatible with CLI permission model | README (partial), package.json (partial) | High; arch mismatch |
| codex | Rust monorepo (codex-rs); pnpm 10.33; Bazel; Node ≥22 | Plan→implement→verify cycle driven by Rust binary; OpenAI subscription locked; CLI + desktop (BETA) | Single executor; ChatGPT Plus/Pro subscription model | `CODEX_SANDBOX` env vars; module <500 LoC discipline; clippy/rustfmt as guardrails | No persistent memory layer documented; LSP for Rust diagnostics | Single-executor output; no debate; no cross-review | None (Bun↔Rust mismatch) | Sandbox env-var pattern + module-size discipline as anti-pattern study only — defer | Rust/Bazel build incompatible with Bun stack; provider lock-in | README, AGENTS.md, package.json | High; build mismatch (study only) |
| gptme | Python 3.10+; Poetry + uv; setuptools; PyPI + CLI/Server/ACP | Conversation loop with step cycling; tool auto-discovery; LogManager session persistence | Generic LLM interface (OpenAI/Anthropic/local); ToolSpec with execute(); no provider capability contract | Hook-based confirmations for destructive actions; pre-execution consent; no sandbox (direct shell/file access) | LogManager session persistence; message history replay; no semantic search | Implicit via tool-execution order + user confirmations; no debate | None | Tool confirmation hook UX may inform M14 reviewer-panel UX — defer | Stateless conversation lacks workflow persistence; tool-centric DAG mismatch | AGENTS.md, pyproject.toml, gptme/tools/ | Medium; paradigm mismatch |
| maestro | Pure bash orchestrator; no formal PM; macOS launchd + iMessage notifications | 3-phase session (plan→implement→review) per agent; file-based PAUSE/STOP/NEEDS_INTERVENTION gates; persistent state.json; perpetual heartbeat | Opus 4.6 orchestrator (non-negotiable) + multi-reviewer wave (Codex/Gemini/Copilot); 5 specialized agents | File gates (PAUSE blocks impl, STOP halts session, NEEDS_INTERVENTION); 3-source verification | 4-file persistent memory (session/findings/observations/decisions) survives restart; iMessage push | Multi-model verification wave; 3-source must-agree gate; cross-check reviewer (Codex as Pair Reviewer) | File-based gate signals + 3-source verification + Opus-default policy | None — patterns are absorbed; remaining surfaces (iMessage/launchd) are macOS-only | Opus-locked default (code-oz uses flexible routing); macOS-bound surfaces | README, CLAUDE.md head, .env.example | Low; already-borrowed |
| Mimir | Node + TS; npm workspaces (MCP, HTTP, VS Code, frontend); Docker multi-container | LangGraph/LangChain agent chains; hierarchical TODO + Graph-RAG memory; bin/run-workflow | Pluggable LLM config loader; agents as graph nodes (neo4j/Graphology); list-models/validate-agent/create-agent | Graph isolation per TODO scope; declarative agent modes (chain, http, mcp); no file sandbox | Graph-based memory (neo4j/Graphology); task↔context↔memory linking; Graph-RAG semantic search; TODO hierarchy | Implicit sequential agent-chain validation; no explicit debate | None (Graph-RAG differs from code-oz repo_context model) | Graph-RAG memory could inform M16+ cross-run memory — defer indefinitely | Hard neo4j/Graphology dep would conflict with single-binary | package.json, CLAUDE.md, orchestrator/ | Medium; heavy deps |
| opencode | Bun 1.3.13 + TS monorepo (console, web, desktop, sdk); turbo orchestration; catalog deps | TUI-first CLI loop; provider-agnostic executor; two agents (build full-access, plan read-only) switchable via Tab; LSP integration | Build agent (full perms) vs plan agent (read-only, ask-before-bash); general subagent for complex search | Nested permission model: build=default; plan asks-before-bash and denies edits; deny-by-default ergonomics | LSP query support; command palette search; agent state in runtime context; no persisted memory | Plan agent read-only; relies on user judgment; no formalized verification loop | bun --compile + MCP host/client + permission system | Tab-switchable agent role (build/plan) — defer until M14 needs interactive panel UX | Detailed per-file/per-command permissions beyond two-role model | README, package.json | Low-Medium; already-borrowed core |
| pi-mono | Bun + TS; ES modules; biome 2.3.5 linter; tsx; husky | Interactive coding agent CLI (pi-coding-agent) with tool calling + state management; Slack bot integration; vLLM GPU pod CLI; TUI; web components | Unified multi-provider LLM API (pi-ai); agent runtime with tool calling + state | No internal auth/permission manifest; multi-provider via external API keys | Streaming event model (core contribution); event-driven knowledge federation | None — agent runtime executes; verification layer added by consumer | Streaming event model + multi-provider abstraction | Continue maintaining as dependency-pattern reference | None — already adopted | README/package.json review only | Low; already-borrowed |
| skills | TypeScript + Node; npm + pnpm; Markdown SKILL.md + optional `.ts` runner | Skill entry via `/grill-me` or intent mapping; "grill-with-docs" pattern; CONTEXT.md domain-language memo | Skills as prompts (not agents); optional `.ts` runners for CLI commands | Bash-native execution from `.ts` runners; no sandbox; script-scoped file access | CONTEXT.md = ubiquitous-language memo; progressive disclosure | None explicit; results are script output + Claude interpretation | None (skills repo is reference-only) | `CONTEXT.md` (domain glossary) for code-oz role taxonomy — defer until M13 role-cost design | Skill-driven execution (would add new dispatcher surface; M16+) | README, CLAUDE.md (partial) | Low (CONTEXT.md only); High (skill dispatcher) |

## Decision buckets

### Borrow-now

None. Every gap that would justify a "borrow-now" call already has a code-oz contract that fits the locked one-authority-per-milestone discipline.

### Borrow-later (revisit at the named milestone)

- **Effort levels (ARIS lite/balanced/max/beast).** Defer until M14 panel design or VERIFY-depth tuning.
- **Domain-language memo (skills `CONTEXT.md`).** Defer until M13 role-cost design needs a glossary surface.
- **Hook event naming (claude-code).** Defer until M14 panel hooks need a vocabulary.
- **Tab-switchable agent role (opencode build/plan).** Defer until M14 panel UX or a TUI inspector.
- **Multi-provider abstraction over 18 providers (byterover-cli @ai-sdk/*).** Defer until friend-survey post-PE-1 confirms route demand beyond xAI direct. Pattern only, no code.
- **Anti-rationalization tables (agent-skills Common-Rationalizations).** Already partially adopted in CLAUDE.md rule 15 (Scientist sidecars). Surface-level adoption complete.

### Reject

- **agentic-coder (provenance — same content as the leaked claude-code-main, relabeled).** Add to CLAUDE.md influence-library exclusion list.
- **claude-coder (IDE extension paradigm; embedded Anthropic OAuth; single-provider).** Architectural mismatch.
- **agenticSeek autonomous loop (no approval gates; stateless paradigm).**
- **codex template (Rust/Bazel build incompatible with Bun; provider lock-in lesson is anti-pattern study, not a borrow).**
- **Mimir hard neo4j/Graphology dependency (conflicts with single-binary distribution).**
- **byterover-cli daemon architecture + bundled UI (conflicts with file-based gates rule + single-binary distribution).**

### Already borrowed (no change)

- agent-skills (frontmatter + phase taxonomy + Common-Rationalizations)
- opencode (bun --compile + MCP + permission system)
- Archon (IAgentProvider + worktree-per-run)
- pi-mono (streaming event model + multi-provider abstraction)
- maestro (file-based gates + 3-source verification + Opus-default)
- Auto-claude-code-research-in-sleep (cross-family review + 4-round cap + plain-Markdown artifacts + Reviewer Memory)
- claude-code (plugin format + hook event names + filesystem discovery)

### Needs-Codex-debate

None earned in this round. PE-1 + M13–M15 each get their own planning rounds where the borrow-later candidates above can be re-tested against measurable need.

## Clean-room and provenance notes

- **agentic-coder is functionally the same provenance issue as the previously-banned claude-code-main.** Files match the publicly leaked Claude Code source; folder is now under a different name. CLAUDE.md influence-library exclusion list must be updated to either name agentic-coder explicitly or restate the discipline as "any folder whose source matches the 2026-03-31 .map leak is excluded." Latter is more durable against future relabels.
- **byterover-cli license is Elastic 2.0** — clean for pattern borrow but not for code embedding. Code-oz's "no copy-paste, borrow patterns only" rule already covers this; documented for completeness.
- **Every other surveyed template carries either MIT/permissive or no LICENSE file** (in which case pattern-only borrow is the only safe interpretation). No additional clean-room burden beyond CLAUDE.md's existing rule.

## Open questions for Codex

1. Does the agentic-coder finding warrant a stricter exclusion rule in CLAUDE.md ("any folder whose source matches the 2026-03-31 .map leak is excluded, regardless of folder name") rather than enumerating folder names?
2. Should the borrow-later candidates be recorded somewhere durable (e.g., in `docs/research/PATTERNS_PARKING_LOT.md`) so they aren't re-discovered each session?
3. Is there a borrow-now candidate I missed by time-boxing each survey to README + AGENTS + top-level docs + 1–2 obvious files? Specifically — anything in opencode's `console`/`web`/`desktop` workspaces that would matter for PE-1's outbound HTTP discipline?
