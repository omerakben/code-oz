<!-- AUDIT SYSTEM PROMPT — PENDING HUMAN CO-AUTHORSHIP (rule 16). DO NOT SHIP. -->
<!--
  This is a PLACEHOLDER STUB, not the real AUDIT system prompt.

  Rule 16 (locked, CLAUDE.md): "LLM-generated persona prompts are forbidden."
  The real AUDIT methodology prose — how the auditor reads an existing
  repository, what an AUDIT.md must contain, the brownfield-specific analysis
  protocol — is HAND-AUTHORED by a human (Ozzy) collaboratively with Claude in
  a later step. It is NOT generated by an automated pass.

  This file exists ONLY so:
    1. `import auditSystemPath from './audit-system.md' with { type: 'file' }`
       in src/prompts/index.ts resolves at module load.
    2. `composeAuditPromptPure` has a token-bearing template to exercise in the
       composition-time rule-16 invariant test.

  The token layout below MIRRORS verify-system.md so the machinery
  (token-presence guard, universal-rules-first injection) is exercised. The
  surrounding text is deliberately mechanical placeholder, never realistic
  audit-methodology prose. When the co-authored prose lands, the human replaces
  the placeholder paragraphs below while keeping the same token set.
-->

# AUDIT phase — system instructions (PLACEHOLDER STUB)

PLACEHOLDER. The real AUDIT system instructions are pending human
co-authorship (rule 16). Do not treat the text below as persona prose.

## Universal rules

These rules apply to every persona in `code-oz`. Read them before drafting.

{{UNIVERSAL_RULES}}

## Your identity

The persona below describes who you are and how you think.

{{AGENT_BODY}}

## Common rationalizations

Read this before every reply.

{{COMMON_RATIONALIZATIONS}}

## Available tools

You may invoke the following tools (subject to your permissions).

{{AVAILABLE_TOOLS}}

## Output protocol

PLACEHOLDER. The AUDIT.md output contract is authored alongside the persona
prose in the human co-authoring step. When ready, emit a line containing
exactly:

```
{{READY_SIGNAL}}
```
